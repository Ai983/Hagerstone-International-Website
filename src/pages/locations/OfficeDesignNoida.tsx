import LocationPage from "../LocationPage";
import { locationPages } from "@/data/locationPages";

export default function OfficeDesignNoida() {
  return <LocationPage data={locationPages.noida} />;
}
